# Proveedor LLM: "ollama" (por defecto, modelos locales) | "groq" (nube)
LLM_PROVIDER=ollama

# Ollama — solo si LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma4

# Groq — solo si LLM_PROVIDER=groq
GROQ_API_KEY=
